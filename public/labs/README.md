# Lab screenshots

Drop sanitized lab screenshots here (e.g. `sqli-result.png`, `privesc-proof.png`).

Reference them in `src/components/CybersecurityLabs.tsx` in each lab’s `screenshots` array:

```ts
screenshots: [
  { src: "/labs/your-file.png", alt: "Description", caption: "Optional caption" },
],
```
